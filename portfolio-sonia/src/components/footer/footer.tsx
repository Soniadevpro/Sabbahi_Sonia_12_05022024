import Badge from "../badge/badge";

const footer = () => {
    return (
        <div>
            <footer>
            <Badge/>

            </footer>
        </div>
    );
};

export default footer;